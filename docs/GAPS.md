# Гапы и риски HomeCare

Обновлено 17.09.2026 по итогам деплоя и аудита. Статус деплоя по пунктам — в [DEPLOY_CHECKLIST.md](DEPLOY_CHECKLIST.md).

Приоритет: 🔴 блокер запуска для клиентов · 🟠 закрыть до запуска или сразу после · 🟡 плановое · ⚪ к сведению

## A. Открытые

| # | | Проблема | Чем грозит | Что сделать | Решает |
|---|---|---|---|---|---|
| 1 | 🔴 | Не подключён ни один канал доставки кода (WhatsApp / Telegram / SMS) | Клиент не получает код → не может войти и оформить заказ. Сейчас код видит только тот, у кого есть доступ к логам сервера | Завести аккаунт (Meta WhatsApp Cloud API, Telegram Gateway или Twilio) → Админка → Настройки → Подтверждение номера | Продукт — аккаунт и оплата, тех — подключение |
| 2 | 🔴 | Нет домена и HTTPS | Телефоны, адреса и cookie сессии идут открытым текстом; браузер помечает сайт «Не защищено» | Купить домен | Продукт |
| 3 | 🔴 | На этом сервере порты 80/443 заняты nginx соседей → Caddy не сможет выпустить сертификат (схема из README не сработает) | Без решения HTTPS не включить | **(а)** отдельный `server {}` в nginx + certbot → проксирование на 127.0.0.1:8080: быстро, но это правка боевого nginx соседей. **(б)** отдельный небольшой VPS/IP для HomeCare (ежемесячная плата): полная независимость, Caddy сам выпустит сертификат. Рекомендация: **(б)** — соответствует требованию «полностью отдельно» | Продукт — бюджет, тех — перенос (бэкап и восстановление уже проверены) |
| 4 | 🔴 | Юридические страницы — заглушки «Текст документа будет добавлен» (оферта, политика конфиденциальности, правила отмены) | Сбор телефонов и адресов без политики обработки персональных данных; спорные ситуации с отменами | Тексты в Админка → Страницы. Контакты в текстах — плейсхолдерами `{{phone}}`, `{{email}}`, `{{name}}` | Продукт / юрист |
| 5 | 🟠 | Публичный демо-контент: мастера Анна, Мариам, Лусине («Демо-профиль»), баннер «−25%», категории «Скоро» | Клиенты увидят вымышленных мастеров | Заменить или удалить в админке (после фикса seed удалённое не вернётся при обновлении) | Продукт |
| 6 | 🟠 | Не настроены уведомления команде о заказах, отменах и отзывах (Telegram-бот) | Новый заказ можно пропустить | @BotFather → токен и ID чата → Настройки → Уведомления | Продукт — чат, тех — настройка |
| 7 | 🟠 | Бэкапы лежат только на этом же диске | Отказ или потеря сервера = потеря базы и фото | Копия вне сервера: Contabo Object Storage или Backblaze B2 + rclone по расписанию, либо снапшоты Contabo | Продукт — аккаунт и ключи, тех — настройка |
| 8 | 🟠 | Код с серверными доработками есть только на сервере (локальный git без remote). Исходный архив разработчика их не содержит | Новый архив от разработчика затрёт порты, контакты, фиксы бэкапов и seed; при портах 80/443 Caddy не стартует | Приватный репозиторий (GitHub), запушить `/opt/homecare`, разработчик продолжает от этой версии | Продукт — репозиторий и доступы, тех — перенос |
| 9 | 🟠 | Нет мониторинга и алертов (сайт недоступен, бэкап не снялся, заполнился диск) | О падении узнаем от клиентов | Минимум: бесплатный UptimeRobot / Better Stack на `http://37.60.236.202:8080/api/health`; алерт об ошибке бэкапа — после пункта 6 | Тех (после согласования сервиса) |
| 10 | 🟠 | Публично показаны тестовые личные контакты, и `ADMIN_PHONE` совпадает с публичным телефоном | Личный номер и почта попадут к спамерам; номер владельца известен всем — это цель для подбора кода | При появлении бизнес-контактов заменить `CONTACT_*`; вход владельца — на непубличном номере | Продукт — номера, тех — замена за 1 минуту |
| 11 | 🟠 | VPN после установки Docker: правила файрвола целы, но живой трафик не проверен (на момент теста не было подключённых клиентов) | Если что-то не так — VPN-клиенты без интернета | Подключиться к VPN один раз и открыть любой сайт; при проблеме — бэкап правил в `/root/pre-docker-backup-2026-09-16-1453/` | Вы (1 минута) |
| 12 | 🟡 | Сайт закрыт от поисковиков (`ROBOTS_TAG=noindex, nofollow`) | Если забыть при запуске — сайт не появится в поиске | При запуске на домене: `ROBOTS_TAG=all` → `docker compose up -d` | Тех, по сигналу продукта |
| 13 | 🟡 | `/en` и `/am` открываются (с русским текстом), хотя языки выключены | После открытия индексации — дубли страниц в поиске | Перевести и включить языки или закрыть эти пути до `ROBOTS_TAG=all` | Продукт — переводы, тех — закрытие путей |
| 14 | 🟡 | Оплата картой не подключена (только наличные) | Часть клиентов не закажет | ArCa vPOS через банк / Idram / Telcell (в roadmap README) | Продукт — договор, тех — интеграция |
| 15 | 🟡 | Защита входа: код стал 6-значным, но нет суточной блокировки номера и оповещения о неудачных попытках; сессия живёт 60 дней; у персонала нет второго фактора | Остаточный риск захвата аккаунта персонала | Блокировка номера на сутки после N неудач, алерт владельцу, более короткие сессии для персонала | Тех (доработка) |
| 16 | 🟡 | Docker не обновляется автоматически (не входит в unattended-upgrades) | Уязвимости Docker остаются до ручного обновления | Раз в месяц `apt upgrade` в окно обслуживания (перезапуск контейнеров ~30 с) | Тех |
| 17 | 🟡 | Сборка на этом VPS медленная (первая ~40 мин) и нагружает общий сервер | Замедление соседей во время обновления | Обновлять вне пиковых часов; в перспективе — сборка образа в CI и только скачивание на сервер | Тех |
| 18 | 🟡 | Кэш сборки Docker растёт (сейчас 3,7 ГБ) | Постепенное заполнение диска | Раз в квартал `docker builder prune -f` | Тех |
| 19 | 🟡 | Нет CI: 23 unit-теста запускаются вручную, e2e-сценарии (Playwright) не автоматизированы | Регрессии заметим поздно | Тесты в CI при подключении репозитория (пункт 8) | Тех |
| 20 | ⚪ | Docker публикует порты в обход ufw | Любой новый `ports:` в compose сразу доступен из интернета | Правило: наружу — только Caddy; базу и app не публиковать | — |
| 21 | ⚪ | Настоящая перезагрузка сервера не проводилась (заденет соседей) | — | Проверено имитацией через перезапуск Docker; при плановой перезагрузке — `docker compose ps` | — |
| 22 | ⚪ | `POSTGRES_PASSWORD` нельзя просто сменить в `.env`; смена `SESSION_SECRET` разлогинит всех | Потеря доступа к базе при неаккуратной смене | Порядок — README → «Эксплуатация» | — |
| 23 | ⚪ | Бэкап снимается при каждом старте контейнера и далее раз в 24 ч, а не в фиксированный ночной час | — | При необходимости — расписание на фиксированное время | — |

## B. Закрыто в ходе деплоя и аудита

| # | Было | Стало | Как проверено |
|---|---|---|---|
| 1 | compose жёстко занимал 80/443 — конфликт с nginx соседей | порты в `.env` (`HTTP_BIND`/`HTTPS_BIND`), сайт на 8080 | соседи 200, nginx не менялся |
| 2 | Первый бэкап пустой (373 байта): снимался до миграций | `backup` ждёт завершения `migrate` | бэкап 9 КБ, 23 таблицы |
| 3 | Неудачный `pg_dump` молча записывал битый файл поверх хорошего | запись во временный файл + проверка конца дампа + переименование | сбой не затёр хороший бэкап |
| 4 | После перезагрузки бэкап при недоступной базе не снимался бы сутки | до 12 повторов раз в 5 минут | имитация перезагрузки: повтор через 5 мин успешен |
| 5 | Фото из админки (том `uploads`) не бэкапились | ежедневный `uploads-*.tar.gz` | архив распакован, владелец файлов сохранён |
| 6 | Бэкапы с персональными данными были читаемы всеми пользователями сервера | каталог 700, файлы 600 | `ls -l` |
| 7 | Инструкция восстановления в README ломалась (заливка в непустую базу) | исправлена | восстановление во временную базу: 23 таблицы совпали построчно |
| 8 | Демо-мастера, баннер, категории и страницы возвращались после **каждого** деплоя (seed) | демо-данные заливаются один раз (флаг `_seed`) | повторный деплой: `skipped` |
| 9 | Настройка «длина кода 4–6» сломана: форма отправляла код на 4-й цифре | форма берёт длину кода с сервера | сквозной тест с 6-значным кодом |
| 10 | Код входа из 4 цифр: номер владельца подбирался в среднем за ~17 дней | код 6 цифр (в среднем ~4,5 года подбора); меняется в Настройки → Подтверждение номера | код в логе — 6 цифр, вход успешен |
| 11 | `CRON_SECRET` был виден в списке процессов сервера (`ps`) | передаётся файлом заголовков | `ps`: 0 вхождений |
| 12 | cron при недоступном app ждал 15 минут | повтор через 60 секунд | после перезапуска вызов прошёл |
| 13 | Логи Docker без ограничений | ротация 5 × 10 МБ | `docker inspect` |
| 14 | Нет лимитов памяти на общем сервере | лимиты на все контейнеры | `docker stats` |
| 15 | Тестовый сайт с личными контактами был открыт для индексации | `X-Robots-Tag: noindex, nofollow`, переключатель `ROBOTS_TAG` | `curl -I` |
| 16 | Не было базовых защитных заголовков; раскрывались `Server` и `X-Powered-By` | `nosniff`, `X-Frame-Options`, `Referrer-Policy`; `Server` и `X-Powered-By` убраны | `curl -I` |
| 17 | README: health-URL указывал на nginx соседей; инструкция домена не работает на общем сервере; не было порядка обновления без git | README обновлён | — |
| 18 | Запрос: контакты в одном месте | блок `CONTACT_*` в `.env` → футер, WhatsApp, плейсхолдеры в страницах; поля в админке заблокированы | страница и админка, 4 unit-теста |
| 19 | Запрос: дизайн будет меняться | все цвета — токены в `src/app/theme.css`, логотип — `<Logo />`, правила в `DESIGN.md`; внешний вид не изменился | CSS содержит токены, хардкод цветов убран |
