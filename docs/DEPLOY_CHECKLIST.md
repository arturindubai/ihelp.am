# Чек-лист деплоя iHelp

Сервер Contabo `37.60.236.202`, каталог `/opt/homecare`. Обновлён 17.09.2026 по итогам деплоя и аудита.
Задачи до полноценной работы сервиса, гапы и риски — в [GAPS.md](GAPS.md).

**Сайт:** http://37.60.236.202:8080/ru · **Админка:** http://37.60.236.202:8080/ru/admin · **Control Center:** http://37.60.236.202:8080/ru/admin/control

Статусы: ✅ сделано и проверено · ⚠️ сделано с оговоркой · ⏳ нужно ваше действие · ➕ пункта не было в исходной инструкции

## 0. Принципы (общий сервер)

На сервере уже работают чужие проекты, HomeCare их не касается:

| Сосед | Что это | Как HomeCare от него отделён |
|---|---|---|
| nginx :80/:443 | liacontentos.com, aistudiolia.com, arturoganesian.com | HomeCare на своём порту 8080, конфиги nginx не менялись |
| pm2 (10 процессов) | LIA dev/prod, ai-blog | HomeCare в Docker, pm2 не трогаем |
| WireGuard wg0/wg1/wg2 + dnsmasq | VPN | правила файрвола VPN сохранены (бэкап до установки Docker) |

HomeCare изолирован: отдельный каталог, docker-проект `homecare` (своя сеть и тома `homecare_*`), свой Postgres, лимиты памяти, свой git.

## 1. Разведка (до изменений)

| Пункт | Статус | Факт |
|---|---|---|
| ОС, RAM, диск | ✅ | Ubuntu 24.04.3, 8 vCPU, RAM 23 ГБ, swap 4 ГБ, свободно 86 ГБ из 145 |
| Docker / Compose | ✅ | не были установлены |
| Порты 80/443 | ✅ | nginx соседей — занимать нельзя. 8080 и 8443 свободны |
| Существующие контейнеры и тома | ✅ | не было |
| ufw | ✅ | активен: 22, 80, 443/tcp + UDP-порты VPN |
| Неучтённое в задаче | ✅ ➕ | ещё 2 сайта в nginx, pm2, VPN — согласовано, работаем изолированно |

## 2. Подготовка

| Пункт | Статус | Факт |
|---|---|---|
| Бэкап правил файрвола до установки Docker | ✅ ➕ | `/root/pre-docker-backup-2026-09-16-1453/` (iptables, ip6tables, ufw, wg, pm2) |
| Docker | ✅ | Docker 29.8.1, Compose v5.5.1 через get.docker.com |
| Swap | ✅ | не нужен (RAM 23 ГБ, swap 4 ГБ уже был) |
| ufw: 22, 80, 443 | ✅ | уже были открыты, SSH не затронут |
| ufw: 8080/tcp | ✅ | добавлен с комментарием `HomeCare (docker caddy)` |
| Docker публикует порты в обход ufw | ✅ ➕ | наружу опубликован только 8080; 8443 — только localhost; Postgres не опубликован. Проверено снаружи: 8443 и 5432 закрыты |
| Файрвол после установки Docker | ✅ ➕ | сверка с бэкапом: ни одно прежнее правило не удалено, политики FORWARD и sysctl не изменились |

## 3. Код

| Пункт | Статус | Факт |
|---|---|---|
| Архив → `/opt/homecare` | ✅ | скачан с Google Drive, оригинал в `/root/homecare-mvp.zip` |
| README прочитан до запуска | ✅ | нюанс: compose жёстко занимал 80/443 → вынесено в `HTTP_BIND`/`HTTPS_BIND` |
| История изменений | ✅ ➕ | локальный git в `/opt/homecare`, первый коммит — оригинальный архив. Remote нет — GAPS → DEV-1 |
| Ревью кода на скрытые проблемы | ✅ ➕ | время (Asia/Yerevan) считается корректно; healthcheck проверяет базу; найдены и исправлены 2 бага (seed, длина кода) |

## 4. Настройки `.env`

| Пункт | Статус | Факт |
|---|---|---|
| `POSTGRES_PASSWORD`, `SESSION_SECRET`, `CRON_SECRET` | ✅ | `openssl rand -hex 32`, три разных значения по 64 символа |
| `ADMIN_PHONE` | ⚠️ | `+37441014713` — тестовый (ваш личный), совпадает с публичным телефоном — GAPS → AUTH-4 |
| `APP_URL` | ✅ | `http://37.60.236.202:8080` |
| `SITE_ADDRESS` | ✅ | `:80` — это порт **внутри** контейнера Caddy; наружу он проброшен на 8080 |
| `HTTP_BIND` / `HTTPS_BIND` | ✅ ➕ | `8080` / `127.0.0.1:8443` |
| `COOKIE_SECURE` | ✅ | `false` (нет HTTPS) |
| `OTP_DEV_MODE` | ✅ | `false`; проверено внутри контейнера и сквозным тестом: код в браузер не отдаётся |
| `CONTACT_*` | ⚠️ ➕ | единое место контактов; сейчас тестовые (ваш телефон и почта) — GAPS → CONTENT-3, README → «Контакты» |
| `ROBOTS_TAG` | ✅ ➕ | `noindex, nofollow` — сайт закрыт от поисковиков до запуска |
| `BACKUP_KEEP_DAYS` | ✅ ➕ | `14` |
| Права на `.env` | ✅ | `600`, секреты нигде не выводились |

## 5. Запуск

| Пункт | Статус | Факт |
|---|---|---|
| `docker compose up -d --build` | ✅ | первая сборка ~40 мин (медленный диск VPS), повторные быстрее за счёт кэша |
| `migrate` завершился успешно | ✅ | `Exited (0)`, миграция `20260916100042_init` применена |
| `app` healthy | ✅ | healthcheck `/api/health` делает запрос в базу |
| Seed не возвращает удалённые демо-данные | ✅ ➕ | при повторном деплое: `Seed: demo data already applied, skipped` |
| Проверка типов и тесты | ✅ ➕ | `tsc --noEmit` без ошибок, `vitest` 24/24 (5 новых тестов на контакты) |

## 6. Проверка

| Пункт | Статус | Факт |
|---|---|---|
| `curl -s http://localhost:8080/api/health` | ✅ | `{"ok":true}`. Внимание: `localhost:80` на этом сервере — nginx соседей |
| `curl -I http://37.60.236.202:8080/ru` | ✅ | `200`, по IPv6 тоже `200` |
| `/ru`, `/ru/s/regular-cleaning`, `/ru/login` | ✅ | `200` |
| `/ru/admin` | ✅ | `307` → `/ru/login?next=/admin` |
| Битые ссылки | ✅ ➕ | обход 13 страниц — битых нет |
| Сквозной вход по OTP через Caddy (уровень API, server actions) | ✅ ➕ | чужой Origin отклонён; код не в ответе; код 6 цифр в логе; подмена `X-Forwarded-For` не проходит; неверный и повторный код отклонены; cookie `HttpOnly; SameSite=lax`; админка `200`. Тестовые сессии удалены |
| UI-тест в реальном браузере (headless Chromium, мобильный и десктоп) | ✅ ➕ | 15/15: контакты и кнопка WhatsApp на главной; токены темы применены; код на экране не показывается; на 4-й цифре из 6 форма ничего не отправляет, на 6-й — ровно один запрос; владелец попадает в админку; поля контактов заблокированы; ошибок JS нет. Скриншоты просмотрены. Тестовая сессия удалена |
| Исходящие запросы из контейнера app | ✅ ➕ | WhatsApp Cloud API, Telegram Gateway, Twilio, Telegram API отвечают за 0,2–0,7 с — отправке кодов сеть не помешает |
| Защищённые API | ✅ ➕ | `/api/cron` без секрета → 403, `/api/upload` без входа → 403 |
| Контакты | ✅ ➕ | в футере «+374 41 014 713», «WhatsApp +374 41 014 713», email; кнопка WhatsApp на главной; в админке поля заблокированы с подсказкой `CONTACT_*` |
| Заголовки | ✅ ➕ | `X-Robots-Tag: noindex, nofollow`, `nosniff`, `X-Frame-Options`, `Referrer-Policy`; `Server` и `X-Powered-By` скрыты |
| Контейнер `cron` | ✅ | `{"ok":true,...}` раз в 15 минут |
| Контейнер `backup` | ✅ | `db-YYYY-MM-DD.sql.gz` + `uploads-YYYY-MM-DD.tar.gz`, права 600 |
| Восстановление из бэкапа | ✅ ➕ | дамп развёрнут во временную базу: все 23 таблицы совпали с боевой построчно; архив фото распаковывается |
| Соседи после всех работ | ✅ | 3 сайта → 200, pm2 10/10 online, `nginx -t` ок, файлы nginx не менялись |
| VPN после установки Docker | ⏳ | правила wg0 на месте, но живой трафик проверить нельзя — активных клиентов не было. **Подключитесь к VPN один раз** — GAPS → RISK-1 |

## 7. Автозапуск

| Пункт | Статус | Факт |
|---|---|---|
| `restart: unless-stopped` | ✅ | у всех сервисов, кроме одноразового `migrate` |
| `systemctl is-enabled docker containerd` | ✅ | `enabled` |
| Имитация перезагрузки | ✅ ➕ | `systemctl restart docker`: всё поднялось само, app healthy за 31 с, cron повторил вызов, бэкап после отказа базы повторился через 5 мин и снялся; файрвол и соседи в порядке |
| Настоящий reboot сервера | ⚠️ | не делался — затронул бы соседей; при плановой перезагрузке проверить `docker compose ps` |

## 8. Эксплуатация

| Пункт | Статус | Факт |
|---|---|---|
| Ротация логов | ✅ ➕ | 5 × 10 МБ на сервис |
| Лимиты памяти | ✅ ➕ | app/db/migrate 1 ГБ, backup 512 МБ, caddy 256 МБ, cron 64 МБ (факт: app ~100 МБ, db ~75 МБ) |
| Бэкап ночью + проверка восстановления | ✅ ➕ | 03:30 по Еревану; раз в неделю дамп разворачивается во временную базу и сверяется; отметки в базе → тех-алерты. Проверено на живой базе, в том числе на битом дампе |
| Тех-алерты | ✅ ➕ | ошибки сайта, бэкап старше 26 ч или с ошибкой, проваленная проверка восстановления, диск < 10%, подбор кода, сбой доставки кода. Проверено: алерты об ошибке 500, просроченном бэкапе и подборе кода сработали (в лог — бот не подключён) |
| Шифрование ключей интеграций | ✅ ➕ | AES-256-GCM, ключ `SETTINGS_ENCRYPTION_KEY` в `.env`; проверено на живой базе: в базе только шифротекст |
| Обновление, откат, smoke-тест | ✅ ➕ | `deploy/update.sh`, `deploy/rollback.sh`, `deploy/smoke.sh`; первая выкатка через скрипт поймала ошибку сборки до перезапуска — прод не пострадал |
| Бэкап вне сервера | ⏳ | нет — GAPS → DB-1 |
| Мониторинг и алерты | ⏳ | нет — GAPS → MON-1, NOTIFY-6 |
| Документация | ✅ ➕ | README (домен на общем сервере, обновление, восстановление, эксплуатация), DESIGN.md, этот чек-лист, GAPS.md, PERSONAL_DATA.md, TEAM_GUIDE.md |

## Команды

```bash
# Первый вход в админку: на /ru/login ввести ADMIN_PHONE, выбрать любой канал, затем код (6 цифр):
docker compose -f /opt/homecare/docker-compose.yml logs app | grep otp

# Проверить, что всё работает (ничего не меняет)
cd /opt/homecare && deploy/smoke.sh https://liacontentos.com https://aistudiolia.com https://arturoganesian.com

# Обновление (после git commit) и откат
cd /opt/homecare && deploy/update.sh https://liacontentos.com https://aistudiolia.com https://arturoganesian.com
cd /opt/homecare && deploy/rollback.sh

# Статус и логи
cd /opt/homecare && docker compose ps
docker compose logs --tail 50 app
docker compose logs backup cron
docker compose logs app | grep notify:tech        # тех-алерты, пока бот не подключён

# Бэкап прямо сейчас
cd /opt/homecare && docker compose exec -T backup sh /backup.sh once

# Смена контактов: правка CONTACT_* в .env, затем (без пересборки)
cd /opt/homecare && docker compose up -d
```

## Чек-лист повторного деплоя (обновление)

1. Если код пришёл новым архивом: распаковать во временную папку, `rsync -a --exclude .env --exclude backups --exclude .git <папка>/ /opt/homecare/`, затем `git -C /opt/homecare diff` — **не потерять серверные доработки**: порты (`HTTP_BIND`), контакты, скрипты `deploy/`, фиксы seed, формы входа, уведомлений.
2. `git -C /opt/homecare add -A && git -C /opt/homecare commit -m "…"` — `update.sh` не запускается с незафиксированными правками.
3. Вне пиковых часов (сборка нагружает общий сервер): `deploy/update.sh https://liacontentos.com https://aistudiolia.com https://arturoganesian.com`.
   Скрипт сам: бэкап → образы для отката → сборка → запуск и миграции → ожидание healthy → smoke-тест (контейнеры, `OTP_DEV_MODE=false`, ключ шифрования, страницы, защита `/api/cron`, превью ссылок, `X-Robots-Tag`, свежесть бэкапа, соседние сайты) → очистка.
4. Упал на сборке — прод не тронут, смотреть вывод. Упал smoke-тест — `docker compose logs --tail 100 app migrate`, при необходимости `deploy/rollback.sh`.
5. Если обновление меняло схему базы — `docker compose logs migrate | tail -5`: миграция применена, seed пишет `skipped`.
