# Control Center

Страница управления проектом внутри админки HomeCare: бэклог с требованиями, статусы задач, комментарии, история и живое состояние системы.

**Адрес:** http://37.60.236.202:8080/ru/admin/control (после подключения домена — `<домен>/ru/admin/control`)
**Доступ:** вход по вашему номеру телефона, роли «Владелец» и «Администратор». Отдельных паролей нет.
Пока каналы доставки кода не подключены, владелец входит по секретной ссылке `/api/auth/link?token=…` (переменная `ADMIN_LOGIN_TOKEN` в `.env`). Ссылку держите в закладках и не пересылайте: по ней входят как владелец. После подключения канала кода ссылку отключаем — задача AUTH-7.

## Что на странице

- **Сводка:** сколько задач открыто, сколько в работе и заблокировано.
- **Прогресс по этапам:** «Первые клиенты», «Публичный запуск», «Рост», «Позже» и то, что уже работает. Клик по этапу фильтрует список.
- **Состояние системы:** когда снимался бэкап, проходила ли проверка восстановления, работают ли фоновые задачи, сколько места на диске, подключены ли каналы кода входа, чат команды и тех-чат, включён ли HTTPS и индексация, работает ли API для агентов, объём данных.
- **Задачи:** поиск, фильтры по этапу, статусу, области, слою (бэк/фронт), приоритету и тому, кто делает. Статус меняется прямо в списке.
- **Карточка задачи:** цель, подробности, критерии приёмки, что нужно от продукта, зависимости в обе стороны, ссылки на документы, комментарии и история изменений.

## Как ведётся бэклог

Тексты задач, требования и зависимости лежат в коде (`src/server/backlog.ts`) и заливаются в базу при каждом обновлении. Статусы, исполнители, причины блокировки и комментарии остаются такими, какими их ведут в интерфейсе, — обновление их не затирает.

- Добавить или переписать задачу: правка `src/server/backlog.ts` → `deploy/update.sh`.
- Печатная версия бэклога: [BACKLOG.md](BACKLOG.md), генерируется из того же файла (`npx tsx scripts/backlog-md.mts > docs/BACKLOG.md`).
- Ключ задачи (например, `AUTH-1`) менять нельзя: по нему связаны зависимости, история и комментарии.

Статусы: **Бэклог → В работе → На проверке → Готово**, отдельно **Заблокирована** с причиной.

## API для агентов

Сделано по образцу Command Center в LIA: агент берёт задачу в аренду, работает, отчитывается. Закрывает задачу всегда человек.

Включается ключом `CC_AGENT_KEY` в `.env` (пусто — API выключено). Все запросы с заголовком `x-cc-key`.

```bash
# список открытых задач бэкенда
curl -s -H "x-cc-key: $KEY" "http://127.0.0.1:8080/api/cc?area=back"

# взять следующую задачу (аренда 60 минут, зависимости учитываются)
curl -s -X POST -H "x-cc-key: $KEY" -H 'Content-Type: application/json' \
  -d '{"action":"claim","agent":"worker-1","area":"back"}' http://127.0.0.1:8080/api/cc

# продлить аренду
curl -s -X POST -H "x-cc-key: $KEY" -H 'Content-Type: application/json' \
  -d '{"action":"heartbeat","agent":"worker-1","key":"ARCH-8"}' http://127.0.0.1:8080/api/cc

# отчитаться и вернуть задачу человеку на проверку
curl -s -X POST -H "x-cc-key: $KEY" -H 'Content-Type: application/json' \
  -d '{"action":"report","agent":"worker-1","key":"ARCH-8","text":"Что сделано и что проверить","status":"review"}' http://127.0.0.1:8080/api/cc
```

Правила:

- Агент берёт только задачи в статусе «Бэклог», у которых закрыты все зависимости.
- Пока идёт аренда, другой агент ту же задачу не возьмёт; просроченная аренда освобождает задачу.
- Отчёт агента виден в карточке задачи как комментарий, смена статуса попадает в историю.
- Статус «Готово» агент поставить не может — это делает человек после проверки.
- Выкладка на сервер агентам не передаётся: обновление запускает человек (`deploy/update.sh`).

Самого демона-воркера (в LIA он поднимает процессы `claude -p` по настройкам пулов) в HomeCare пока нет — это задача **DEV-6** в бэклоге: нужны решения по бюджету и правам доступа.

## Если что-то не сходится

- Задача исчезла из списка — проверьте фильтр «Только открытые» и фильтры по этапам.
- В списке есть задача, которой нет в коде, — при деплое об этом пишется строка `Backlog: … not in code: …` (`docker compose logs migrate`).
- Страница не открывается — у вашей роли нет доступа: нужна роль «Владелец» или «Администратор» (Админка → Сотрудники).
